<%= form_with(model: organization, local: true) do |form| %>
  <% if organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :organization_name %>
  </div>

  <div class="field">
    <%= form.label :base_uri %>
    <%= form.text_field :base_uri, id: :organization_base_uri %>
  </div>

  <div class="field">
    <%= form.label :full_domain %>
    <%= form.text_field :full_domain, id: :organization_full_domain %>
  </div>

  <div class="field">
    <%= form.label 'Harvest OAuth Client ID' %>
    <%= form.text_field :harvest_id, id: :organization_harvest_id %>
  </div>

  <div class="field">
    <%= form.label :harvest_key %>
    <%= form.text_field :harvest_key, id: :organization_harvest_key %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :organization_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
